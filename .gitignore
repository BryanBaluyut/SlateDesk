# Build artifacts
/bin/
frontend/dist/
frontend/node_modules/

# Embedded SPA build output — populated by `make build` from frontend/dist;
# only the .gitkeep is tracked so go:embed has a file on fresh clones.
internal/web/dist/*
!internal/web/dist/.gitkeep

# Local environment
.env
*.log

# Default local attachment blob store (SLATEDESK_DATA_DIR=./data)
/data/

# Claude session config/notes — contains infra details, never commit
.claude/
CLAUDE.md
